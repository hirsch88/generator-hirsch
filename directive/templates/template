/**
 * <%= capitalizedName %>
 * @namespace <%= modulePath %>
 */
(function() {
    'use strict';

    angular
        .module('<%= modulePath %>.<%= lowercaseName %>', [
            <%= modules %>
        ])
        .directive( '<%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>', <%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>Directive );

    /**
     * @memberOf <%= modulePath %>
     * @namespace <%= modulePath %>.<%= lowercaseName %>
     *
     * @description
     * <%= description %>
     *
     * @example <<%= prefix %>-<%= lowerModuleName %>-<%= lowercaseName %>></<%= prefix %>-<%= lowerModuleName %>-<%= lowercaseName %>>
     */
    function <%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>Directive() {
        return {
            restrict:         '<%= restrict %>',<% if (hasTemplate) { %>
            templateUrl:      '<%= templateUrl %>',<% } %><% if (hasController) { %>
            controller:       <%= moduleName %><%= capitalizedName %>Controller,
            controllerAs:     '<%= lowerModuleName %><%= capitalizedName %>',
            bindToController: true<% } %><% if (hasLinkFnc && hasController) { %>,<% } %><% if (hasLinkFnc) { %>
            link:             <%= moduleName %><%= capitalizedName %>Link<% } %>

        };
        <% if (hasLinkFnc) { %>
        function <%= moduleName %><%= capitalizedName %>Link(scope, element, attrs) {

            // code will be here ...

        }<% } %>
    }
    <% if (hasController) { %>
    function <%= moduleName %><%= capitalizedName %>Controller(<%= dependencies %>) {
        var vm = this;

    }<% } %>


}());
