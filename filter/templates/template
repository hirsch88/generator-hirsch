/**
 * <%= capitalizedName %>
 * @namespace <%= modulePath %>
 */
(function() {
    'use strict';

    angular
        .module('<%= modulePath %>.<%= lowercaseName %>', [
            <%= modules %>
        ])
        .filter('<%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>', <%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>Filter);

    /**
     * @memberOf <%= modulePath %>
     * @namespace <%= modulePath %>.<%= lowercaseName %>
     *
     * @description
     * <%= description %>
     */
    function <%= prefix %><%= capitalizedModuleName %><%= capitalizedName %>Filter(<%= dependencies %>) {

        return Filter;
        ////////////////////////

        function Filter(input) {
            input = input || '';

            // code goes here

            return input;
        }

    }


}());
