/**
 * <%= capitalizedName %>
 * @namespace <%= modulePath %>
 */
(function() {
    'use strict';

    angular
        .module('<%= modulePath %>.<%= lowercaseName %>', [
            <%= modules %>
        ])
        .filter('<%= moduleName %><%= capitalizedName %>', <%= moduleName %><%= capitalizedName %>);

    /**
     * @memberOf <%= modulePath %>
     * @namespace <%= moduleName %><%= capitalizedName %>
     *
     * @description
     * <%= description %>
     */
    function <%= moduleName %><%= capitalizedName %>(<%= dependencies %>) {

        return Filter;
        ////////////////////////

        function Filter(input) {
            input = input || '';

            // code goes here

            return input;
        }

    }


}());
